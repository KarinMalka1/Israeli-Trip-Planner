"""Pure domain logic: regions, shabbat, schedule arithmetic, distance lookup.

Nothing here touches FastAPI or the network, so every rule in SPEC section 3
is testable without spinning up an app.
"""
